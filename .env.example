APP_NAME=Laravel
APP_ENV=local
APP_NAME="ICM Sponsor DB"
APP_ENV=production
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_DEBUG=false
APP_URL=https://sponsor.icm.or.id

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

# --------------------------------------------------------------------------
# Logging
# --------------------------------------------------------------------------
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug
# production: error | debug mode lokal: debug
LOG_LEVEL=error

# --------------------------------------------------------------------------
# Database — Production: MySQL wajib, SQLite hanya dev/test
# --------------------------------------------------------------------------
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sponsor_db
DB_USERNAME=root
DB_USERNAME=icm_user
DB_PASSWORD=

# VPS produksi 50 user: wajib MySQL (DB_CONNECTION=mysql). SQLite hanya untuk testing lokal.
# Pragma SQLite (dipakai bila DB_CONNECTION=sqlite - dev/test saja)
# Pragma SQLite (hanya aktif bila DB_CONNECTION=sqlite — dev/test)
DB_BUSY_TIMEOUT=5000
DB_JOURNAL_MODE=WAL
DB_SYNCHRONOUS=NORMAL

# --------------------------------------------------------------------------
# Session, Cache, Queue
# --------------------------------------------------------------------------
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# VPS 50 user baca: MySQL row-lock aman untuk session. Cache file lebih ringan dari database.
# Production: file lebih ringan dari database untuk 50 user baca
# Upgrade ke redis bila perlu horizontal scale
CACHE_STORE=file
# CACHE_PREFIX=
# CACHE_PREFIX=icm

MEMCACHED_HOST=127.0.0.1

# --------------------------------------------------------------------------
# Redis (opsional — aktifkan bila ingin session/cache di redis)
# --------------------------------------------------------------------------
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
# --------------------------------------------------------------------------
# Mail
# --------------------------------------------------------------------------
MAIL_MAILER=smtp
MAIL_SCHEME=tls
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_ADDRESS="noreply@sponsor.icm.or.id"
MAIL_FROM_NAME="${APP_NAME}"

# --------------------------------------------------------------------------
# AWS / Storage (opsional)
# --------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_DEFAULT_REGION=ap-southeast-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# --------------------------------------------------------------------------
# Vite Asset
# --------------------------------------------------------------------------
VITE_APP_NAME="${APP_NAME}"

# --------------------------------------------------------------------------
# Security hardening hints (tidak dibaca Laravel secara langsung,
# tapi digunakan oleh nginx/server config — dicantumkan sebagai reminder)
# --------------------------------------------------------------------------
# FORCE_HTTPS=true        → AppServiceProvider sudah handle via URL::forceScheme
# TRUSTED_PROXIES=*       → tambahkan di config/app.php jika di balik load balancer
